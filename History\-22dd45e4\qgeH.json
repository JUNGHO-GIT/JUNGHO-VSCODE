{
  "configurations": [
  {
    "name": "Launch jsp",
    "request": "launch",
    "type": "chrome",
    "url": "http://localhost:9000/${workspaceFolderBasename}/${projectBasename}/${fileBasename}",
    "webRoot": "${workspaceFolder}"
  }
  ]
}